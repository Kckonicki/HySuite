$C = "../Common.ui";

$C.@PageOverlay {
  $C.@Container {
    Anchor: (Width: 500, Height: 450);

    #Title {
      Group {
        Anchor: (Height: 50);

        Label #TitleLabel {
          Style: (
            FontSize: 24,
            LetterSpacing: 2,
            RenderUppercase: true,
            RenderBold: true,
            TextColor: #ccb588,
            HorizontalAlignment: Center,
            VerticalAlignment: Center
          );
          Text: "Server Warps";
        }
      }
    }

    #Content {
      LayoutMode: Top;
      Padding: (Top: 10, Left: 15, Right: 15, Bottom: 15);

      Group #WarpListContainer {
        FlexWeight: 1;
        LayoutMode: TopScrolling;
        ScrollbarStyle: $C.@DefaultScrollbarStyle;

        Group #WarpList {
          LayoutMode: Top;
        }
      }

      Group #Separator {
        Anchor: (Height: 1, Top: 15);
        Background: (Color: #2b3542);
      }

      Group {
        LayoutMode: Left;
        Anchor: (Height: 44, Top: 15);

        Group { FlexWeight: 1; }

        $C.@TextButton #CloseButton {
          Text: "Close";
          Anchor: (Width: 120, Height: 44);
        }
      }
    }
  }
}

$C.@BackButton {}
